#include <stdio.h>

int main() {
    int yuto;

    printf("数字を入力してください：");
    scanf_s("%d", &yuto);

    printf("%dの乗算表:\n", yuto);
    for (int i = 1; i <= 10; ++i) {
        printf("%d × %d = %d\n", yuto, i, yuto * i);
    }
    return 0;
}
